# ==========================================
# 1. INTEL GPU EXPORTER (Port 9100)
# ==========================================
---
apiVersion: v1
kind: Service
metadata:
  name: intel-gpu-exporter
  namespace: default
spec:
  ports:
    - port: 9100
      targetPort: 8080
      name: metrics
  selector:
    app: intel-gpu-exporter
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: intel-gpu-exporter
  namespace: default
spec:
  selector:
    matchLabels:
      app: intel-gpu-exporter
  template:
    metadata:
      labels:
        app: intel-gpu-exporter
    spec:
      hostPID: true
      nodeSelector:
        feature.node.kubernetes.io/pci-0300_8086.present: "true"
      containers:
        - name: exporter
          image: ghcr.io/clambin/intel-gpu-exporter:latest
          args:
            - "--interval=5s"
            - "--device=drm:/dev/dri/card0"
          ports:
            - containerPort: 8080
          securityContext:
            privileged: true
          volumeMounts:
            - name: dev-dri
              mountPath: /dev/dri
      volumes:
        - name: dev-dri
          hostPath:
            path: /dev/dri

# ==========================================
# 2. AMD GPU EXPORTER (Port 9401)
# ==========================================
---
apiVersion: v1
kind: Service
metadata:
  name: amd-gpu-exporter
  namespace: default
spec:
  ports:
    - port: 9401
      name: metrics
  selector:
    app: amd-gpu-exporter
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: amd-gpu-exporter
  namespace: default
spec:
  selector:
    matchLabels:
      app: amd-gpu-exporter
  template:
    metadata:
      labels:
        app: amd-gpu-exporter
    spec:
      nodeSelector:
        feature.node.kubernetes.io/pci-0300_1002.present: "true"
      containers:
        - name: exporter
          image: ghcr.io/jkroepke/amd-gpu-exporter:latest
          args: ["--web.listen-address", ":9401"]
          ports:
            - containerPort: 9401
          securityContext:
            privileged: true
          volumeMounts:
            - name: sys
              mountPath: /sys
      volumes:
        - name: sys
          hostPath:
            path: /sys

# ==========================================
# 3. NVIDIA GPU EXPORTER (Port 9400)
# ==========================================
---
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
---
apiVersion: v1
kind: Service
metadata:
  name: nvidia-gpu-exporter
  namespace: default
spec:
  ports:
    - port: 9400
      name: metrics
  selector:
    app: nvidia-gpu-exporter
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nvidia-gpu-exporter
  namespace: default
spec:
  selector:
    matchLabels:
      app: nvidia-gpu-exporter
  template:
    metadata:
      labels:
        app: nvidia-gpu-exporter
    spec:
      runtimeClassName: nvidia
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: feature.node.kubernetes.io/pci-0300_10de.present
                    operator: Exists
              - matchExpressions:
                  - key: feature.node.kubernetes.io/pci-0302_10de.present
                    operator: Exists
      containers:
        - name: exporter
          image: nvcr.io/nvidia/k8s/dcgm-exporter:3.3.0-3.2.0-ubuntu22.04
          env:
            - name: DCGM_EXPORTER_LISTEN
              value: ":9400"
          ports:
            - containerPort: 9400
          securityContext:
            capabilities:
              add: ["SYS_ADMIN"]
